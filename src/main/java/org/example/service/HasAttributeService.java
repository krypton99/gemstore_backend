package org.example.service;

import org.example.model.HasAttribute;
import org.example.model.Response;

public interface HasAttributeService {
    public Response hasAttribute(HasAttribute hasAttribute);
}
